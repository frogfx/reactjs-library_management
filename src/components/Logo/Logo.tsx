import React from "react";
import * as s from "./StyleLogo";

const Logo: React.FC = () => {
   return (
      <s.Logo>
         Library <span>Management</span>
      </s.Logo>
   );
};

export default Logo;
